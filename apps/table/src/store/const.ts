export const enum NameSpace {
  Common = 'common'
}

export enum Types {
  Dataset = 'dataset',
  Parameters = 'columns',
  Length = 'length',
  Default = 'default',
  Generate = 'generate',
  Reset = 'clear'
}
